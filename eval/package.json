{
  "name": "pi-tdd-eval",
  "version": "0.1.0",
  "description": "Eval suite for pi-tdd extension",
  "type": "module",
  "scripts": {
    "eval": "tsx eval.ts",
    "test": "vitest run",
    "view": "tsx -e \"import('pi-do-eval').then(m => new m.EvalServer('.', 4242).start())\""
  },
  "dependencies": {
    "pi-do-eval": "file:../../pi-do-eval"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.1",
    "@types/node": "^25.6.0"
  }
}
